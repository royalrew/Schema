services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: toreboda
      POSTGRES_USER: toreboda
      POSTGRES_PASSWORD: toreboda
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U toreboda"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
